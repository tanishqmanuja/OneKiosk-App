import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.starkinc.onekiosk',
  appName: 'OneKiosk',
  webDir: 'www',
  bundledWebRuntime: false,
  backgroundColor: '#00000000',
};

export default config;
